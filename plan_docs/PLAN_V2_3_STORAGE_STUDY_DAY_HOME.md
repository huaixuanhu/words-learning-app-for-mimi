# V2.3 数据加载、学习日与 Home 修复

Created: 2026-09-13 AEST
Last updated: 2026-09-13 AEST

Source plan:
- `plan_docs/PLAN_V2_MASTER.md`

Derived from:
- `plan_docs/PLAN_V2_2_REVIEW_CADENCE_CARD_AUDIO.md`
- `ARCHITECTURE.md`
- 用户 2026-09-13 的 V2.3 修复请求，以及随后确认的每日 06:00 刷新时间。

Scope:
- 在本地 `codex/v2.3` 分支调查 Library 空白，修复云端失败被显示为空本地词库的问题和已确认的连接异常处理缺口。
- 根据学习者已设时区，将新的学习日调整为 06:00 至次日 06:00；保留历史计划窗口、事件与两条学习轨道的独立状态。
- 数据加载修复后，简化 Home Insight：左侧展示每日新学与复习的不同词数；右侧展示最新 Review load（复习量）和 Recall（回忆概率）估计。
- 本地代码、测试、文档与使用合成数据的页面验证。
- 修正正式构建发现的原有 TTS 路由辅助函数导出位置；原服务选择与处理逻辑逐字保留。

Non-Scope:
- 此计划不授权 Production（生产环境）部署、数据库写入、清理、恢复、迁移，GitHub 推送、合并或凭据修改。
- 不改变 AI/TTS 服务、额度、自然日计账规则、鉴权或数据发送范围。
- 不改写旧计划窗口、历史事件和已有 FSRS 参数；不将当前读取错误解释成已经证实的数据删除。

Authority and safety:
- 当前、目标与工作分类均为 Tier 3；用户已授权三项本地修复和建立分支，随后明确确认本地提交与现有线上连接凭据读取，仅进行连接状态、Schema 和记录数量的只读核查，并要求排查相关隐患。
- 初次浏览器检查未读取凭据；后续核查沿用已有 Keychain（钥匙串）内的项目固定凭据，只在进程内使用，不输出、复制或保留密钥及连接串。
- 本地修改尚未部署，不能用本地测试宣称线上数据已经恢复。

Exit criteria:
- 明确区分成功的云端读取、服务器明确指定的本地运行和读取失败；失败时保留学习者选择、提供重试并保护数据写入。
- 空闲数据库连接异常可被处理，查询和事务错误仍向调用方报告，写入不自动重放。
- 午夜不再拆开新的学习日；06:00、夏令时、旧窗口过渡、过期请求和页面休眠后刷新均有覆盖。
- 左图新学与复习采用不同词数，保留轨道隔离；右侧为当前数据计算的估计，缺少依据时不给出伪造数值。
- 本地验证完成；线上恢复、数据完整性核对和发布状态单独报告。

## Stage 2.3.1 Library 调查和加载修复

2026-09-13 只读证据：
- 当前 canonical domain 的 Home 显示全零，Library 在空搜索、All Words 下显示 `0 shown / 0 total`。
- Vercel 当前 Production 为 `dpl_HXmxx55SPvmhAwhFwaV6eRmTtySg`，Git `main` / `3a64aa2f413549b2fc5c2292722c1f5df456abab`。
- Vercel 日志在 `2026-09-13T09:23:32.322Z` 等多个时间记录 `GET /api/storage/data` 为 500；展开的请求显示 `Uncaught Exception`，调用路径为 Pool `idleListener` → WebSocket 错误，Node.js 退出码 129。`09:31:19.163Z` 的 `/api/storage/health` 为 503。
- 本地 `use-vocabulary-data.ts` 将失败读取统一返回 `null`，首次加载再安装空的本地默认数据，并覆盖所选学习者。Library 本身不按日期过滤全部词汇。
- 本地 `postgres/client.ts` 创建共享 Pool（连接池）时未注册空闲连接错误处理。
- 证据上限：确认连接读取失败和错误处理缺陷；底层连接故障原因、数据库当前内容完整性尚未确认。

先完成这些本地修复及失败恢复测试，再进入 Home 展示修改。

## Stage 2.3.2 06:00 学习日

- 新增学习日专用日期函数；AI/TTS 继续使用原自然日函数。
- 以本地钟面 06:00 计算边界，分别解析起止时间，覆盖夏令时产生的 23/25 小时窗口。
- 旧窗口保持原样。过渡时优先继续尚未结束的旧计划；如午夜后目标日期已被关闭的旧计划占用，创建从旧窗口结束到次日 06:00 的衔接计划，通常为 30 小时，夏令时对应 29/31 小时。这种一次性过渡的第一个 06:00 不会再次刷新；后续恢复 06:00→06:00。时区和日期线切换也保留旧日期键，衔接窗口不可重叠或改写历史。
- Home/Study 在窗口结束与回到页面时刷新；已过期学习请求继续被拒绝，不静默改记到另一日。

## Stage 2.3.3 Home Insight

- 保留现有配色、导航、轨道切换和 7/14 天选择。桌面左右排列，窄屏按活动、估计顺序堆叠。
- 左侧每个学习日两根柱：`New words`、`Reviewed`。同词同日重复尝试不重复累计。
- 右侧按用户后续选择保留按日期和回忆概率分组的小图，精简重复标签，显示最近计算时间。`Today` 为本学习日结束前的到期词数，包含更早已到期词；Recall 分组保留具备有效历史的词数。
- 沿用原生按钮、键盘焦点、已选状态与减少动效设置。验证加载、错误、空数据和有数据状态，320–1280 px 范围内避免横向溢出。

## Stage 2.3.4 验证与交接

- 定向验证数据读取失败、连接异常、日期窗口、复习与新学计数、估计计算及页面显示。
- 完成 governance、lint、typecheck、完整 Vitest、三种 fixture（测试数据）备份检查、build 与 diff 检查。
- 同步 Architecture、V2 master、Changelog 和 AI log；清楚区分本地完成、线上恢复和发布授权。

## 当前状态与验证

- 三项本地修复与调查后的保护补充已实现，Production 仍运行 V2.2；直接原因已确认为 Neon 网络传输额度超限，线上恢复及数据完整性核对未关闭。
- 初始候选 661 项测试通过；补充保护后的 `npm run test` 为 102 个文件、673 个测试通过。既有远程 Postgres integration（集成测试）文件及 1 项测试跳过。
- `npm run lint`、`npm run typecheck`、三种 `backup:dry-run:*fixture` 均通过。
- `npm run governance:check` 通过严格预检和 22 项治理检查；`git diff --check` 通过。
- 在无凭据临时副本中检查真实本地 Home 的空数据和加载行为；临时视觉页使用 42 个合成词、97 条事件验证 7/14 天、Recognition/Active 切换、分组图、错误状态、键盘焦点与 Retry。1280 px / 320 px 页面已查看，320 px 的内容宽度未溢出。重试的数据保留行为另由真实 provider（数据加载组件）状态测试覆盖，未将视觉宿主等同于线上恢复验证。
- 14 天手机图表最初的星期标签过窄，已改为日号并补充明确日期范围后重新查看。
- 正式构建发现 HEAD 中已有的 TTS 路由额外导出不符合 Next.js 路由约定；将 `createTtsServiceResolver` 原样移至同目录 `service-resolver.ts`，服务、缓存、额度选择和 POST 处理逻辑与原实现逐字比较一致。12 项相关测试通过。
- `npm run build -- --webpack` 已在排除全部环境/凭据文件的临时源码副本中通过，临时视觉页在构建前移除。未用默认 Turbopack 构建结果作声明。
- Backup v4 / Schema 6 往返测试已确认旧午夜计划、30 小时过渡和正常 06:00 后继计划的全部 6 条 Track 记录、历史归属和冻结证据保持一致。
- 独立复核修正了休眠后无效时钟、时区过渡及 Home 恢复后仍残留旧错误提示；Library 错误恢复路径和词数统计未发现新的数据丢失问题。
- 初始候选已完成实现和验证，后续在用户明确授权后保存为本地提交 `0f242d5`。提交前的暂存差异检查发现新增 TTS 文件末尾多余空行，已修正；当前继续完成已授权的只读调查及连接保护补充。未推送、未部署，未执行数据库写入、恢复或迁移。

## Stage 2.3.5 连接故障归因与隐藏风险

### 已确认的直接原因

- 2026-09-13 `10:37:12Z`、`10:38:25Z` 两次有界只读探测使用原有 Keychain 身份，目标项目、`main` 分支与 Sydney endpoint（连接端点）的哈希均与 V2.2 留存证据一致。
- 首个 `BEGIN ... READ ONLY` 请求被 SQLSTATE `53000` 拒绝，错误内容在进程内归类为 `network-transfer-quota-exceeded`。没有开始数据数量查询，没有读取学习内容；确认原因后停止重复 SQL 探测。
- Neon 控制台 API 的当前账户类型为 `free_v3`；本期 `2026-09-01T00:00:00Z` 至 `2026-10-01T00:00:00Z` 的 `data_transfer_bytes = 6,330,077,483`，约 6.33 GB。Compute（计算资源）仅为 3,756 CU-seconds，当前故障指向传输额度。
- `main` 仍为 `ready`，逻辑大小为 44,376,064 bytes，连接端点为 `idle`，没有被删除或禁用。最后活动为 `2026-09-12T15:44:14Z`，最近暂停操作在 `15:52:15Z` 开始、次秒完成；操作记录没有说明超额触发时刻，不能把该暂停操作单独认定为配额触发证据。
- 当前因传输额度无法执行记录计数。数据库存在和逻辑大小不证明逐条数据完整；留存的 2026-09-03 备份基线为 282 个词、125 个复习状态、441 条复习事件、22 张表，仅作为恢复后比较的历史锚点。
- 因果链：Neon 传输额度超限阻断查询 → 原应用连接异常未被完整处理 → 原客户端将加载失败显示为空词库。前两轮只读日志和本轮直接连接证据相互补充；没有证据支持实际删库或词汇被清空。

### 已确认的流量放大路径

- 每张卡激活时的 `refreshPrompt` 和每次评分都会读取该学习者完整的数据快照，包括全部词汇、复习历史、每日计划、创建事实、导入记录及 AI 记录；正常单卡至少两次整份读取，队列进入和重试另外计入。
- 每次评分会通知其他打开的页面；每个页面再获取一份包含全部有效学习者的完整快照。浏览器增量更新没有减少这些数据库到服务器的传输。
- 普通 Home 解析会在计划稳定后停止变化；Insights 的分钟计时仅进行本地计算。没有发现 V2.2 普通单页持续自动查询的循环或定时数据库任务。
- 这些读取方式足以构成随历史增长而加重的明确风险，但当前没有逐请求传输统计，不能将 6.33 GB 全部精确归因给某一接口、页面或学习操作。
- 合成模型按历史基线数量构造 282 个词、441 条事件、1,486 条创建事实，整份 API JSON 约 1.06 MB；估计两次数据库文本行传输约 1.35 MB/卡，不含其余协议流量。留存备份的汇总字节元数据也显示 13 张相关表的 canonical JSON（规范化 JSON）合计约 1.21 MB。这些仅说明量级，不替代本月真实用量归因。

### 本轮本地补充与待处理风险

| 状态 | 风险与处理 |
| --- | --- |
| 已补充本地保护 | 事务占用的连接也可能异步报错；新增事务期间错误监听。连接失败或回滚失败时丢弃连接，保留原错误，写入不自动重放。连接获取超时为 10 秒，并接入已有 Vercel Pool 生命周期助手。 |
| 本地加载边界补充 | 对损坏响应提供可重试错误，避免安装快照时异常逃逸；较早请求返回时保留另一个页面刚选定的学习者。 |
| 尚未实施 | 按卡片/队列所需数据读取，避免每次完整拉取历史；跨页同步合并通知并缩小同步内容。这是减流的主要后续改造方向，单纯调整 Pool 无法减少完整查询结果的体积。 |
| 单独记录，尚未修复 | 真正的本地模式遇到损坏/不支持的旧存档仍可能返回空数据，下一次保存覆盖原始内容；已用合成数据复现。需要保留原始存档并阻止普通写入。该路径不等于本次云端额度故障。 |
| 单独记录，尚未修复 | 数据写入提交成功后，再读取响应快照失败，会把已成功写入显示为失败；人工重试部分操作可能重复执行。需要明确“结果待核对”并以一致的请求标识核查，不能盲目重放。 |
| 证据限制 | 浏览器备份来自缓存，服务器整份快照由独立查询组成；并发修改时存在时效性/一致性限制。目前没有证据证明某份真实备份损坏。 |
| 恢复证据缺口 | 工作区能找到的最新独立加密备份验证记录为 2026-09-03，距本次故障约 10 天，超过每周备份策略的间隔。可能存在工作区外的新备份，但本轮没有其证据；当前额度限制也阻止新逻辑备份读取。 |

下一阶段减流应保留 server-owned plan/token（服务器控制的计划与令牌）、两个 Track 的独立历史和 FSRS 计算依据，在合成数据下验证请求数与返回字节量后再发布；不能通过删除历史来换取流量下降。

### 证据与外部事实

- 忽略且权限受限的本地证据：`local_artifacts/v2-3-diagnosis/probe-result-initial.json`、`probe-result.json`、`control-plane-result.json`。只包含身份哈希、汇总用量、状态和脱敏错误，无凭据或学习内容。
- [Neon network transfer](https://neon.com/docs/introduction/network-transfer)：Free 每月 5 GB，超额会暂停 compute，直到下一计费周期或升级；建议缩小查询结果并分页。该文档也说明不提供逐查询或逐连接的传输细分。
- [Neon project details API](https://api-docs.neon.tech/reference/getproject)：当前项目设置、状态与用量的只读来源。
- [Vercel connection pooling](https://vercel.com/kb/guide/connection-pooling-with-functions)：函数暂停与连接池清理的官方指导；这是本轮加固依据，不是此次额度超限的替代原因。

## 下一授权边界

用户随后已明确授权本地提交和现有凭据读取，初始候选已提交为 `0f242d5`。本轮已确认额度故障和相关读取放大路径，补充保护已通过本地验证；两个只读事务均在首条请求被拒绝，未提取学习内容或记录数量。

恢复查询的现有选项是等到下个用量周期（API 当前记录为 `2026-10-01T00:00:00Z`），或由用户另行决定并授权付费套餐调整；当前未执行升级。减少按卡片全量读取能降低未来用量，但代码部署不能解除已经触发的额度限制。下一步应先完成按需读取改造，在查询恢复后执行已授权的只读计数/完整性检查，再决定候选发布。部署、远程推送以及任何恢复或数据写入均未授权。
