# V2.3 数据加载、学习日与 Home 修复

Created: 2026-09-13 AEST
Last updated: 2026-09-13 AEST

Source plan:
- `plan_docs/PLAN_V2_MASTER.md`

Derived from:
- `plan_docs/PLAN_V2_2_REVIEW_CADENCE_CARD_AUDIO.md`
- `ARCHITECTURE.md`
- 用户 2026-09-13 的 V2.3 修复请求，以及随后确认的每日 06:00 刷新时间。

Scope:
- 在本地 `codex/v2.3` 分支调查 Library 空白，修复云端失败被显示为空本地词库的问题和已确认的连接异常处理缺口。
- 根据学习者已设时区，将新的学习日调整为 06:00 至次日 06:00；保留历史计划窗口、事件与两条学习轨道的独立状态。
- 数据加载修复后，简化 Home Insight：左侧展示每日新学与复习的不同词数；右侧展示最新 Review load（复习量）和 Recall（回忆概率）估计。
- 本地代码、测试、文档与使用合成数据的页面验证。
- 用户随后明确要求减少按卡全量读取、防止重复保存与持续查询，并增加常态化备份。最新选择为 Neon 云端承担每日备份，本机降为每月一次的独立加密副本；具体配置和执行证据由 Stage 2.3.11 更新。
- 用户决定升级现有 Vercel 管理的 Neon 集成至基础 Launch 套餐；沿用同一项目，升级后进行已授权的只读核查。
- 修正正式构建发现的原有 TTS 路由辅助函数导出位置；原服务选择与处理逻辑逐字保留。

Non-Scope:
- 此计划不授权 Production（生产环境）部署、数据库写入、清理、恢复、迁移，GitHub 推送、合并或凭据修改。现有 Neon Launch 套餐升级与既有目的地的常态化加密备份已由后续请求授权；不升级 Vercel 团队套餐，不添加其他付费服务。
- 不改变 AI/TTS 服务、额度、自然日计账规则、鉴权或数据发送范围。
- 不改写旧计划窗口、历史事件和已有 FSRS 参数；不将当前读取错误解释成已经证实的数据删除。

Authority and safety:
- 当前、目标与工作分类均为 Tier 3；用户已授权三项本地修复和建立分支，随后明确确认本地提交与现有线上连接凭据读取，仅进行连接状态、Schema 和记录数量的只读核查，并要求排查相关隐患。
- 初次浏览器检查未读取凭据；后续核查沿用已有 Keychain（钥匙串）内的项目固定凭据，只在进程内使用，不输出、复制或保留密钥及连接串。
- 后续授权覆盖按需读取、防重复与防循环修复、现有 Production 数据的加密备份及其本地恢复校验、当前 Neon 集成基础套餐升级。支付资料由用户直接在 Vercel 填写。数据库恢复写入与应用发布仍是独立边界。
- 本地修改尚未部署，不能用本地测试宣称线上数据已经恢复。后续云端备份配置、既有目的地的每月加密副本和 US$5 提醒由用户明确授权；US$10 总账单硬上限需按平台实际能力说明，不将提醒冒充封顶。

Exit criteria:
- 明确区分成功的云端读取、服务器明确指定的本地运行和读取失败；失败时保留学习者选择、提供重试并保护数据写入。
- 空闲数据库连接异常可被处理，查询和事务错误仍向调用方报告，写入不自动重放。
- 午夜不再拆开新的学习日；06:00、夏令时、旧窗口过渡、过期请求和页面休眠后刷新均有覆盖。
- 左图新学与复习采用不同词数，保留轨道隔离；右侧为当前数据计算的估计，缺少依据时不给出伪造数值。
- 本地验证完成；线上恢复、数据完整性核对和发布状态单独报告。

## Stage 2.3.1 Library 调查和加载修复

2026-09-13 只读证据：
- 当前 canonical domain 的 Home 显示全零，Library 在空搜索、All Words 下显示 `0 shown / 0 total`。
- Vercel 当前 Production 为 `dpl_HXmxx55SPvmhAwhFwaV6eRmTtySg`，Git `main` / `3a64aa2f413549b2fc5c2292722c1f5df456abab`。
- Vercel 日志在 `2026-09-13T09:23:32.322Z` 等多个时间记录 `GET /api/storage/data` 为 500；展开的请求显示 `Uncaught Exception`，调用路径为 Pool `idleListener` → WebSocket 错误，Node.js 退出码 129。`09:31:19.163Z` 的 `/api/storage/health` 为 503。
- 本地 `use-vocabulary-data.ts` 将失败读取统一返回 `null`，首次加载再安装空的本地默认数据，并覆盖所选学习者。Library 本身不按日期过滤全部词汇。
- 本地 `postgres/client.ts` 创建共享 Pool（连接池）时未注册空闲连接错误处理。
- 证据上限：确认连接读取失败和错误处理缺陷；底层连接故障原因、数据库当前内容完整性尚未确认。

先完成这些本地修复及失败恢复测试，再进入 Home 展示修改。

## Stage 2.3.2 06:00 学习日

- 新增学习日专用日期函数；AI/TTS 继续使用原自然日函数。
- 以本地钟面 06:00 计算边界，分别解析起止时间，覆盖夏令时产生的 23/25 小时窗口。
- 旧窗口保持原样。过渡时优先继续尚未结束的旧计划；如午夜后目标日期已被关闭的旧计划占用，创建从旧窗口结束到次日 06:00 的衔接计划，通常为 30 小时，夏令时对应 29/31 小时。这种一次性过渡的第一个 06:00 不会再次刷新；后续恢复 06:00→06:00。时区和日期线切换也保留旧日期键，衔接窗口不可重叠或改写历史。
- Home/Study 在窗口结束与回到页面时刷新；已过期学习请求继续被拒绝，不静默改记到另一日。

## Stage 2.3.3 Home Insight

- 保留现有配色、导航、轨道切换和 7/14 天选择。桌面左右排列，窄屏按活动、估计顺序堆叠。
- 左侧每个学习日两根柱：`New words`、`Reviewed`。同词同日重复尝试不重复累计。
- 右侧按用户后续选择保留按日期和回忆概率分组的小图，精简重复标签，显示最近计算时间。`Today` 为本学习日结束前的到期词数，包含更早已到期词；Recall 分组保留具备有效历史的词数。
- 沿用原生按钮、键盘焦点、已选状态与减少动效设置。验证加载、错误、空数据和有数据状态，320–1280 px 范围内避免横向溢出。

## Stage 2.3.4 验证与交接

- 定向验证数据读取失败、连接异常、日期窗口、复习与新学计数、估计计算及页面显示。
- 完成 governance、lint、typecheck、完整 Vitest、三种 fixture（测试数据）备份检查、build 与 diff 检查。
- 同步 Architecture、V2 master、Changelog 和 AI log；清楚区分本地完成、线上恢复和发布授权。

## 初始候选状态与验证（追加结果见 Stage 2.3.10）

- 三项本地修复与调查后的保护补充已实现，Production 仍运行 V2.2；直接原因已确认为 Neon 网络传输额度超限，线上恢复及数据完整性核对未关闭。
- 初始候选 661 项测试通过；补充保护后的 `npm run test` 为 102 个文件、673 个测试通过。既有远程 Postgres integration（集成测试）文件及 1 项测试跳过。
- `npm run lint`、`npm run typecheck`、三种 `backup:dry-run:*fixture` 均通过。
- `npm run governance:check` 通过严格预检和 22 项治理检查；`git diff --check` 通过。
- 在无凭据临时副本中检查真实本地 Home 的空数据和加载行为；临时视觉页使用 42 个合成词、97 条事件验证 7/14 天、Recognition/Active 切换、分组图、错误状态、键盘焦点与 Retry。1280 px / 320 px 页面已查看，320 px 的内容宽度未溢出。重试的数据保留行为另由真实 provider（数据加载组件）状态测试覆盖，未将视觉宿主等同于线上恢复验证。
- 14 天手机图表最初的星期标签过窄，已改为日号并补充明确日期范围后重新查看。
- 正式构建发现 HEAD 中已有的 TTS 路由额外导出不符合 Next.js 路由约定；将 `createTtsServiceResolver` 原样移至同目录 `service-resolver.ts`，服务、缓存、额度选择和 POST 处理逻辑与原实现逐字比较一致。12 项相关测试通过。
- `npm run build -- --webpack` 已在排除全部环境/凭据文件的临时源码副本中通过，临时视觉页在构建前移除。未用默认 Turbopack 构建结果作声明。
- Backup v4 / Schema 6 往返测试已确认旧午夜计划、30 小时过渡和正常 06:00 后继计划的全部 6 条 Track 记录、历史归属和冻结证据保持一致。
- 独立复核修正了休眠后无效时钟、时区过渡及 Home 恢复后仍残留旧错误提示；Library 错误恢复路径和词数统计未发现新的数据丢失问题。
- 初始候选已完成实现和验证，后续在用户明确授权后保存为本地提交 `0f242d5`。提交前的暂存差异检查发现新增 TTS 文件末尾多余空行，已修正；当前继续完成已授权的只读调查及连接保护补充。未推送、未部署，未执行数据库写入、恢复或迁移。

## Stage 2.3.5 连接故障归因与隐藏风险

### 已确认的直接原因

- 2026-09-13 `10:37:12Z`、`10:38:25Z` 两次有界只读探测使用原有 Keychain 身份，目标项目、`main` 分支与 Sydney endpoint（连接端点）的哈希均与 V2.2 留存证据一致。
- 首个 `BEGIN ... READ ONLY` 请求被 SQLSTATE `53000` 拒绝，错误内容在进程内归类为 `network-transfer-quota-exceeded`。没有开始数据数量查询，没有读取学习内容；确认原因后停止重复 SQL 探测。
- Neon 控制台 API 的当前账户类型为 `free_v3`；本期 `2026-09-01T00:00:00Z` 至 `2026-10-01T00:00:00Z` 的 `data_transfer_bytes = 6,330,077,483`，约 6.33 GB。Compute（计算资源）仅为 3,756 CU-seconds，当前故障指向传输额度。
- `main` 仍为 `ready`，逻辑大小为 44,376,064 bytes，连接端点为 `idle`，没有被删除或禁用。最后活动为 `2026-09-12T15:44:14Z`，最近暂停操作在 `15:52:15Z` 开始、次秒完成；操作记录没有说明超额触发时刻，不能把该暂停操作单独认定为配额触发证据。
- 当前因传输额度无法执行记录计数。数据库存在和逻辑大小不证明逐条数据完整；留存的 2026-09-03 备份基线为 282 个词、125 个复习状态、441 条复习事件、22 张表，仅作为恢复后比较的历史锚点。
- 因果链：Neon 传输额度超限阻断查询 → 原应用连接异常未被完整处理 → 原客户端将加载失败显示为空词库。前两轮只读日志和本轮直接连接证据相互补充；没有证据支持实际删库或词汇被清空。

### 已确认的流量放大路径

- 每张卡激活时的 `refreshPrompt` 和每次评分都会读取该学习者完整的数据快照，包括全部词汇、复习历史、每日计划、创建事实、导入记录及 AI 记录；正常单卡至少两次整份读取，队列进入和重试另外计入。
- 每次评分会通知其他打开的页面；每个页面再获取一份包含全部有效学习者的完整快照。浏览器增量更新没有减少这些数据库到服务器的传输。
- 普通 Home 解析会在计划稳定后停止变化；Insights 的分钟计时仅进行本地计算。没有发现 V2.2 普通单页持续自动查询的循环或定时数据库任务。
- 这些读取方式足以构成随历史增长而加重的明确风险，但当前没有逐请求传输统计，不能将 6.33 GB 全部精确归因给某一接口、页面或学习操作。
- 合成模型按历史基线数量构造 282 个词、441 条事件、1,486 条创建事实，整份 API JSON 约 1.06 MB；估计两次数据库文本行传输约 1.35 MB/卡，不含其余协议流量。留存备份的汇总字节元数据也显示 13 张相关表的 canonical JSON（规范化 JSON）合计约 1.21 MB。这些仅说明量级，不替代本月真实用量归因。

### 本轮本地补充与待处理风险

| 状态 | 风险与处理 |
| --- | --- |
| 已补充本地保护 | 事务占用的连接也可能异步报错；新增事务期间错误监听。连接失败或回滚失败时丢弃连接，保留原错误，写入不自动重放。连接获取超时为 10 秒，并接入已有 Vercel Pool 生命周期助手。 |
| 本地加载边界补充 | 对损坏响应提供可重试错误，避免安装快照时异常逃逸；较早请求返回时保留另一个页面刚选定的学习者。 |
| 已本地实现 | 卡片提示刷新和评分改为读取当前词、相关计划及窗口内事件。跨页同步合并并限频，后台页面延后；同步快照本身仍是全量，后续分页不是本轮完成声明。 |
| 已本地实现 | 损坏/不支持的本机存档抛出可恢复错误并保留原文，普通保存前重查存档。明确本地运行才能通过有效备份预览与确认恢复，替换前保留原文。 |
| 已本地实现，有边界 | 成功提交后的快照失败返回明确已保存状态；未知结果只补读并保留跨刷新的待核对标记。相同并发请求合并、已一致状态跳过写入。没有新增跨设备的通用命令回执表。 |
| 证据限制 | 浏览器备份来自缓存，服务器整份快照由独立查询组成；并发修改时存在时效性/一致性限制。目前没有证据证明某份真实备份损坏。 |
| 恢复证据缺口 | 工作区能找到的最新独立加密备份验证记录为 2026-09-03，距本次故障约 10 天，超过每周备份策略的间隔。可能存在工作区外的新备份，但本轮没有其证据；当前额度限制也阻止新逻辑备份读取。 |

下一阶段减流应保留 server-owned plan/token（服务器控制的计划与令牌）、两个 Track 的独立历史和 FSRS 计算依据，在合成数据下验证请求数与返回字节量后再发布；不能通过删除历史来换取流量下降。

### 证据与外部事实

- 忽略且权限受限的本地证据：`local_artifacts/v2-3-diagnosis/probe-result-initial.json`、`probe-result.json`、`control-plane-result.json`。只包含身份哈希、汇总用量、状态和脱敏错误，无凭据或学习内容。
- [Neon network transfer](https://neon.com/docs/introduction/network-transfer)：Free 每月 5 GB，超额会暂停 compute，直到下一计费周期或升级；建议缩小查询结果并分页。该文档也说明不提供逐查询或逐连接的传输细分。
- [Neon project details API](https://api-docs.neon.tech/reference/getproject)：当前项目设置、状态与用量的只读来源。
- [Vercel connection pooling](https://vercel.com/kb/guide/connection-pooling-with-functions)：函数暂停与连接池清理的官方指导；这是本轮加固依据，不是此次额度超限的替代原因。

## Stage 2.3.6 按需读取与请求边界

- `refreshPrompt` 与 `recordRating` 改为读取令牌指定计划、当前词、当前 Review Profile（复习配置）及相关时间窗口内记录。保留计划锁、令牌有效期、重复评分拦截、FSRS 依据和跨时区窗口冲突保护。
- 页面恢复与跨页同步合并重复读取，自动通知至少间隔 30 秒，隐藏页面延后；连续三次自动读取失败后暂停。持续成功的外部数据变化最多每分钟两次同步，不声称整个页面生命周期只读三次。
- 学习日边界/时钟失效自动解析间隔至少 60 秒，最多三次；相同学习者的并发解析共享一次请求。Study 请求 15 秒中止等待，无自动写入重放。初次加载、明确数据失效与人工重试继续提供恢复入口。
- 用合成 SQL 返回行验证结果大小不随无关历史线性增长；这类测试不代表真实网络字节或本月流量归因。

## Stage 2.3.7 保存结果与本机恢复保护

- 同时提交相同内容只执行一次，已一致的状态编辑跳过写入。
- 提交成功但后续快照失败，服务端返回明确的已提交状态；前端只补读数据，不重放写入。
- 提交响应丢失时仅进行一次读取核对，未能正向确认的操作阻止后续通用保存，并保留跨页面刷新的待核对标记。此标记限同一浏览器来源与运行环境；学习者选择仍允许，Study 使用既有独立持久化命令保护。没有新增持久化通用命令表，不把未确认结果宣称为严格的任意操作只执行一次。
- 无法读取的本机存档保留原文并阻止普通覆盖；只有明确本地运行才能显示恢复入口。用户选取有效备份、查看摘要并明确确认后，先保留旧原文，再替换本机存档。云端故障不能解锁本地恢复。

## Stage 2.3.8 初始本机每日备份方案（已由 Stage 2.3.11 取代）

- 后续请求触发原 Stage 8.5 的备份升级条件。目标频率从每周提升为每天 07:00 Australia/Melbourne；沿用现有 Keychain、`age` 加密及 `~/Documents/Mimi Vocabulary Backups` 目的地。
- 新包装脚本复用既有固定 Production 身份的备份脚本，每次在隔离的本机 PostgreSQL 17 恢复并比对；远端只读。
- 同日只尝试一次，互斥运行，有界超时；失败、中断和占用不自动重复传输整库。保留现有周备份，增加每日备份，初期不自动删除任何归档。
- 激活前须套餐恢复、首次真实备份通过、脚本已提交且工作区干净。Mac 需运行并可使用 Keychain；这仍是本机定期备份，不构成独立的全天候云端备份服务。
- 既有工具在备份前后源数据发生变化时拒绝归档；日常开发造成工作区不干净也会安全停止并占用当天尝试。每次读取两个全库 JSON 摘要输入和一次 dump，恢复比对在本机执行；备份仍消耗网络传输额度。
- 包装脚本设置 10 分钟总时限，终止独立子进程组并清理该次隔离的 PostgreSQL；无法确认清理时保留锁与文件，需人工处理。中断可能留下未验证的加密归档，不能将其列为已验证恢复点。
- 已创建 Codex 当前任务的每日备份配置 `mimi`，状态为 `PAUSED`。用户明确选择稍后填写支付资料、先完成代码；未运行真实备份，未激活调度。正常成功保持安静，失败/错过/清理未完成才通知。

## Stage 2.3.9 Neon Launch 升级与恢复核查

- 已在 Vercel 原 Neon 集成进入 Change Plan，并选择 Launch。页面确认 Compute 为 US$0.106/CU-hour，Storage 为 US$0.35/GB-month；计划适用于该集成下全部产品。未创建新数据库、迁移项目或升级 Vercel 团队套餐。
- 初次操作停在 Add Payment Method：该集成没有已配置的支付方式，账单姓名和地址为空。用户选择稍后直接填写、先完成代码修复；页面已保留。该历史等待现已关闭，升级与核查结果见 Stage 2.3.11。
- 升级成功后核查当前计划，再执行已批准的固定 Production 身份只读计数、Schema 和孤立/重复关系检查；随后进行首份加密备份和本地恢复比对。

## 当前外部边界

本地提交已有 `0f242d5`、`7a354cd`；后续三项风险修复在同一 `codex/v2.3` 分支完成并按既有授权保存为本地提交。Neon Launch 升级和常态化备份已获得明确用户授权，用户已完成支付资料和升级，数据库读取已恢复。最新云端主备份和每月独立副本状态见 Stage 2.3.11。应用部署、远程推送以及数据库恢复、写入、迁移均未授权。

## Stage 2.3.10 本地追加验证与交接

- 最终完整 Vitest：107 个文件、732 项测试通过，既有远程 Postgres integration 文件/1 项测试跳过。lint、typecheck 和三种 backup fixture dry-run 通过。
- 热路径合成 SQL 验证：刷新提示 6 次语句、评分 14 次语句（均含事务边界），合计从 43 次降到 20 次；0 与 10,000 条无关历史的评分返回大小和查询数一致。这里计量的是测试返回 JSON，不是 Production 网络流量。
- 50 个同时解析请求合并成一次服务端读取；1,600 次页面唤醒事件在八分钟内最多三次边界解析；隐藏十分钟不发起自动解析。跨页 100 个通知合并为一次请求及一次至少 30 秒后的追加同步。
- 通用保存覆盖同内容并发、已一致状态、已提交后读取失败、提交结果不明、15 秒超时、队列阻止、刷新页面后的待核对保护、选择学习者的竞争；归档/恢复和导入内部读取也绑定明确事务结果。
- 本机损坏存档覆盖、替换前原文保留、保存空间失败与备份校验兼容性有定向测试。浏览器额外查看恢复入口 320 px 布局，修正标题及表单文字颜色后复查，无横向溢出。文件校验与真实恢复由组件/存储测试覆盖，视觉页没有执行用户数据恢复。
- 每日备份包装脚本覆盖同日去重、互斥、失败与中断、超时/子进程终止及清理保护；没有执行真实 Production 备份或远程修改。
- 最终无凭据临时副本的 `next build --webpack` 通过；governance 严格预检与 22 项治理测试通过，差异检查通过。临时页面与开发服务器均已清理。
- 保留的实际限制：初次加载和跨页同步仍可读取完整快照；未知通用创建/事件/导入操作可能需要人工核对，未实现跨设备通用 exactly-once（严格只执行一次）；Mac 本机备份不等于全天候独立云服务。Production 仍运行 V2.2，当前分支未推送或部署。

## Stage 2.3.11 云端主备份、每月独立副本与费用提醒

- 用户已自行完成 Vercel 支付资料与 Launch 升级；Vercel 和刷新后的 Neon 均显示 Launch。沿用原项目与数据库，无迁移。
- 当前实施范围：在 Neon 的 Production `main` 配置每天 21:00 UTC 的原生快照，保留 14 天；项目恢复历史提升至 7 天。本机每月 1 日 07:00 Australia/Melbourne 生成并验证一份加密副本，仅对新建的月度专属归档保留最近三份，既有发布恢复点保持原样。云端自动任务独立于 Mac；本机副本仍需 Mac 可用。
- Neon 组织 Spending notifications 已设置为 US$5，80% / 100% 提醒对应 US$4 / US$5。官方和页面均说明该功能不会停止项目；Vercel Spend Management 不覆盖 Marketplace 集成。US$10 总账单硬上限当前不可配置，替代资源额度等待用户选择，不静默启用自动暂停。
- 升级后已完成一次固定 Production 身份的只读核查：Schema 6、22 张表、5,585 个词、363 条复习状态、1,194 条复习事件、39 个导入批次；重复词组、孤立状态和孤立事件均为 0。证据保存在忽略且权限受限的 `local_artifacts/v2-3-diagnosis/probe-result-post-upgrade.json`。这些证据确认当前可读及所检关系，不能证明所有历史逐条完整。
- 云端配置已保存并由固定目标 API 读回确认：`launch_v3`、`history_retention_seconds=604800`，每日 `hour=21`、`retention_seconds=1209600`。首份手动快照创建于 `2026-09-13T11:52:19Z`，完整大小 44,376,064 bytes，过期时间 `2026-09-27T11:52:19Z`。未执行云端恢复演练。忽略证据为 `local_artifacts/v2-3-diagnosis/cloud-backup-settings-result.json`。扩大历史窗口不证明被回收的旧历史重新出现。
- 原生快照存放于 Neon 管理的云端存储，同属当前 Neon 账户边界；独立加密副本存放在 `/Users/anoria/Documents/Mimi Vocabulary Backups`。快照当前价格为 US$0.09/GB-month，恢复历史为 US$0.20/GB-month，按实际占用计费。
- 官方依据：[Backup & Restore](https://neon.com/docs/guides/backup-restore)、[History window](https://neon.com/docs/postgres/backup-restore/history-window)、[Spending notifications](https://neon.com/docs/introduction/spending-notifications)、[Vercel Spend Management](https://vercel.com/docs/spend-management)。未部署应用、推送代码或执行 Production 数据恢复/写入。

- 既有自动任务 `mimi` 已改为每月 1 日 07:00 Australia/Melbourne，初始状态仍为 PAUSED；本机脚本和首次真实备份验证通过后再激活，不保留第二个每日任务。
- 当前原备份目录有 8 个文件，合计 1,476,951 bytes；仅检查文件元数据，全部保留。月度副本使用独立的 `monthly-local-v1` 子目录。月度调度表示每月一次有界尝试：Mac 不可用、Keychain 不可用或工作区不干净时可能错过；失败不会自动反复读取整库，须查看失败回执并明确处理。云端每日计划不依赖这些本机条件。
- 月度本机实现完成：共用原全局互斥锁，每月只尝试一次；当前调用的恢复证据、Production 身份和加密归档校验通过，并原子保存成功回执后，才轮换专属目录内有验证归属的旧副本。拒绝链接目录/文件和损坏证据，失败保留新旧归档，不清理其他档案。
- 本轮定向备份测试 31 项通过，覆盖月度去重、三份轮换、证据/身份/时间绑定、链接文件保护及已有子进程超时/清理；独立代码复核无剩余阻断项。全仓 lint、governance 严格预检及 22 项治理测试通过；产品运行代码未变，Stage 2.3.10 的应用验证仍适用。真实月度备份必须在本地提交后执行，结果另行追加。
- 月度候选已本地提交为 `2a334a0`。首次真实尝试于 `2026-09-13T12:02:14.977Z` 开始，34 秒后返回失败；没有保留已验证月度归档，调度仍为 PAUSED。原失败回执 `local_artifacts/daily-production-backups/2026-09.json` 保留。旧包装脚本丢弃底层错误细节，正在补充仅保留安全错误类别/阶段的诊断，并排查临时目录导致 Unix socket（本机数据库通信文件）路径过长的问题。
- 修正确定的本机问题后，允许一次明确指定的复核重试；保留原失败回执、使用独立重试回执，同月不能再发起第二次复核重试。普通月度调度仍不自动重试，未完成清理或占用中的任务不得借此重试。该受控只读重试属于已授权备份和本机修复范围，不引入远端数据写入或恢复。
- 不含学习数据的隔离 PostgreSQL 17 实测复现：127-byte socket 路径启动失败（无法创建 Unix-domain sockets），同一 cluster（数据库实例）改为 52-byte 路径后启动成功；测试实例已停止并清理。初次沙箱验证受 IPC（进程间通信）限制，获准的本机测试完成后才建立此结论。该证据证明路径缺陷，但不能补回首次真实尝试已丢失的具体失败阶段。
- 修复使用系统 `/tmp` 实际路径下的私有短目录，并预先限制 socket 路径长度；错误输出只保留允许的 code/phase，不输出原始错误。新增独立 `.retry-1.json` 复核重试记录，绑定原失败回执的 SHA-256（校验值），原记录字节保持不变；成功重试按一个成功月份计入轮换。修复后的 40 项备份定向测试、定向 lint 和差异检查通过。
